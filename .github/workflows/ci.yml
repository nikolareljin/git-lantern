name: ci

on:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  python:
    uses: nikolareljin/ci-helpers/.github/workflows/python.yml@0.6.0
    with:
      working_directory: "."
      lint_command: "./scripts/lint.sh"
      test_command: "./scripts/test.sh"
      build_command: "./scripts/build.sh"
